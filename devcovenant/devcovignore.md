# DevCovenant Ignore Patterns
# Paths matching these patterns are skipped by all policy checks.

# Third-party / vendored code
copernican_lib/vendor/**

# Test suites (root and nested)
tests/** **/tests/**
