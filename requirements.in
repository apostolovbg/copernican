-r copernican/runtime-requirements.lock
-r devcovenant/runtime-requirements.lock

# Add repository workspace dependencies below.
# `dependency-management refresh-all` writes `requirements.lock` from this
# file.
# Keep the shipped Copernican runtime reference and the shipped
# DevCovenant runtime reference unless this repository owns a different
# `root_workspace` contract through its active profile stack.

# Independent scientific reference dependency for repository tests only.
# Its shared numerical dependencies stay pinned to the packaged runtime ABI.
camb==1.6.0
numpy==1.26.4
packaging==26.2
scipy==1.12.0
sympy==1.13.0
